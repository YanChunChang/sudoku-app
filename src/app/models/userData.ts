export interface UserData{
    name: string;
    email: string;
    password: string;
}
export interface ScoreData {
    nickname: string;
    playerMode: string;
    playMode: string;
    level: string;
    time: number;
    date: string;
}